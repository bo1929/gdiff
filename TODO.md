* Refactor `gamma.cpp/hpp`, `dist.cpp/hpp`, `map.cpp/hpp`, `detect.cpp/hpp`, and `dim.cpp/hpp` at some point.
* Revisit output formats and CLI.
* Have a map mode where you can specify the direction, too.

